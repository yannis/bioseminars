- obj_sym = object.class.to_s.humanize.downcase
= form_for object, :remote => index?, :html => {:id => 'new_or_edit_form'} do |f|
  %h1
    = "Create a #{obj_sym}"
  = render "#{obj_sym.pluralize}/form", :f => f