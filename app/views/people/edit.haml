- @title ||= "Edit person “#{@person.name}”"
%h2
  = @title
= error_messages_for :person
- form_for :person, :url => person_path(@person), :html => { :method => :put, :class => 'with_blocks'} do |f|
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag 'Update'