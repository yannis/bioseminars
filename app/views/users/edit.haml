- @title ||= "Edit user “#{@user.name}”"
- @user.password = @user.password_confirmation = nil
%h2
  = @title
= error_messages_for :user
- form_for :user, :url => user_path(@user), :html => { :method => :put, :class => 'with_blocks'} do |f|
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag 'Update'
