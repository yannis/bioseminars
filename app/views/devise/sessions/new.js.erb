<%= flash_in_js %>
$('#new_or_edit_form').html("<%= escape_javascript(render('form', :resource => resource)) %>");
$('#new_or_edit_form').effect("highlight", {}, 3000);
hide_ajax_loader();
insert_loaders();