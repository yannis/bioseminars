<%= flash_in_js %>
$('#content aside form').html("<%= escape_javascript(render('form', :resource => resource)) %>");
$('#content aside form').effect("highlight", {}, 3000);
hide_ajax_loader();
insert_loaders();